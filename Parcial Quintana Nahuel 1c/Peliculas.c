#include <stdio.h>
#include <stdlib.h>
#include "Peliculas.h"
#define T 3
#define VACIO 0
#define OCUPADO 1

/*ePeliculas listaDePeliculas[T];
eGenero listaDeGeneros[T];

void hardCodearDatos(ePeliculas listaDePeliculas[], int T,eGenero listaDeGeneros[], int T)
{
    int codigoAux[] = {1,2,3};
    char descripcion[][50]= {"IT","Avengers","Que Paso Ayer"};
    float duracionAux [] = {2.30, 3, 2};
    int idGeneroAux[] = {1,2,3};
    int idAux[] = {1,2,3};
    char nombreGeneros[][50] = {"Terror","Accion","Comedia"};
    int i, j;
    for(i = 0; i < tam; i++)
    {
        listaDePeliculas[i].codigo = codigoAux[i];
        listaDePeliculas[i].duracion = duracionAux[i];
        strcpy(listaDePeliculas[i].descripcion,descripcion[i]);
        listaDeAlumnos[i].estado = OCUPADO;
    }
    for(j = 0; j < T; j++)
    {
        listaDeGeneros[j].id = idAux[j];
        strcpy(listaDeGeneros[j].descripcion,nombreGeneros[j]);
    }
}*/
